# 实验一

- 日期：2024.10.25
- 分析：

> YOLO默认有3个检测头，能够多尺度对目标行检测其检测尺寸分别是：
>
> - P3/8 对应的检测特征图大小为80X80，用于检测大小在8X8以上的目标。
> - P4/16对应的检测特征图大小为40X40，用于检测大小在16X16以上的目标。
> - P5/32对应的检测特征图大小为20X20，用于检测大小在32X32以上的目标。
>
> 但对微小目标检测可能存在检测能力不佳的现象，因此添加一个微小物体的检测头：增加160X160的检测特征图，用于检测4X4以上的目标。
>

- 模型改动：

正常的YOLOv8对象检测模型输出层是P3、P4、P5三个输出层，为了提升对小目标的检测能力，添加第四个输出层（P2）。Backbone部分的结果没有改变，但是Neck跟Header部分模型结构调整如下：


```yaml
......

# YOLOv8.0n backbone
backbone:
  # [from, repeats, module, args]
  - [-1, 1, Conv, [64, 3, 2]]   # 0-P1/2
  - [-1, 1, Conv, [128, 3, 2]]  # 1-P2/4
  - [-1, 3, C2f, [128, True]]   # P2 processed for head
  - [-1, 1, Conv, [256, 3, 2]]  # 3-P3/8
  - [-1, 6, C2f, [256, True]]
  - [-1, 1, Conv, [512, 3, 2]]  # 5-P4/16
  - [-1, 6, C2f, [512, True]]
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 3, C2f, [1024, True]]
  - [-1, 1, SPPF, [1024, 5]]    # 9

# YOLOv8.0n head
head:
  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 6], 1, Concat, [1]]   # cat backbone P4
  - [-1, 3, C2f, [512]]         # 12

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 4], 1, Concat, [1]]   # cat backbone P3
  - [-1, 3, C2f, [256]]         # 15 (P3/8-small)

  - [-1, 1, nn.Upsample, [None, 2, 'nearest']]
  - [[-1, 1], 1, Concat, [1]]   # cat backbone P2
  - [-1, 3, C2f, [128]]         # P2 added to the head
  
  - [-1, 1, Conv, [128, 3, 2]]
  - [[-1, 15], 1, Concat, [1]]  # cat head P3
  - [-1, 3, C2f, [256]]         # 18 (P3/8-small)

  - [-1, 1, Conv, [256, 3, 2]]
  - [[-1, 12], 1, Concat, [1]]  # cat head P4
  - [-1, 3, C2f, [512]]         # 21 (P4/16-medium)	

  - [-1, 1, Conv, [512, 3, 2]]
  - [[-1, 9], 1, Concat, [1]]   # cat head P5
  - [-1, 3, C2f, [1024]]        # 24 (P5/32-large)

  - [[15, 18, 21, 24], 1, Detect, [nc]]  # Detect(P2, P3, P4, P5)

```

- 训练参数：

```python
    model = YOLO('./model.yaml')
    model.load('yolov8n.pt') 

    # Train the model
    model.train(cfg='./cfgs/rope_cfg.yaml',
                data='./datasets/rope_dataset.yaml', 
                epochs=150, 
                imgsz=1024)
```

- 结果：

```
 Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 
   all        118        157      0.366      0.513      0.382      0.152
broken         76        140      0.485      0.571      0.524      0.213
  warp         12         15      0.177      0.467      0.125      0.045
scatter          2          2      0.437        0.5      0.496      0.198
```



# 实验二

- 日期：2024.10.25

- 模型：原生yolov11n

- 结果

```
 Class     Images  Instances      Box(P          R      mAP50  mAP50-95): 
   all        118        157      0.469      0.744      0.601      0.234
broken         76        140      0.265        0.7      0.544      0.211
  warp         12         15      0.198      0.533      0.263     0.0946
scatter          2          2      0.945          1      0.995      0.398

```

